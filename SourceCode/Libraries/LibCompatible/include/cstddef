#pragma once

#include "stddef.h"

namespace std {
  using size_t    = ::size_t;
  using ptrdiff_t = ::ptrdiff_t;

#if __cplusplus >= 201103L
  using nullptr_t = ::nullptr_t;
#endif
} // namespace std
